import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the audit pages under the fixture route. Filled by the audit workstream. */
export const auditQueries: FixtureQueries = {};
