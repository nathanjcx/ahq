import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the calendar pages under the fixture route. Filled by the calendar workstream. */
export const calendarQueries: FixtureQueries = {};
