import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the channels pages under the fixture route. Filled by the channels workstream. */
export const channelsQueries: FixtureQueries = {};
