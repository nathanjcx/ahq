import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the meetings pages under the fixture route. Filled by the meetings workstream. */
export const meetingsQueries: FixtureQueries = {};
