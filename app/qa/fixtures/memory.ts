import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the memory pages under the fixture route. Filled by the memory workstream. */
export const memoryQueries: FixtureQueries = {};
