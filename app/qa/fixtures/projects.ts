import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the projects pages under the fixture route. Filled by the projects workstream. */
export const projectsQueries: FixtureQueries = {};
