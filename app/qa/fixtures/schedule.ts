import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the schedule pages under the fixture route. Filled by the schedule workstream. */
export const scheduleQueries: FixtureQueries = {};
