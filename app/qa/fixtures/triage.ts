import type { FixtureQueries } from '@/components/shared/use-ui-query';

/** Query answers for the triage pages under the fixture route. Filled by the triage workstream. */
export const triageQueries: FixtureQueries = {};
