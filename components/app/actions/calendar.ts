'use client';

/** Calendar actions. Filled by the calendar UI workstream: type, offline stubs, and the hook, kept in step. */
export type CalendarActions = Record<never, never>;

export const offlineCalendarActions: CalendarActions = {};

export function useCalendarActions(): CalendarActions {
  return {};
}
