'use client';

/** Channels actions. Filled by the channels UI workstream: type, offline stubs, and the hook, kept in step. */
export type ChannelsActions = Record<never, never>;

export const offlineChannelsActions: ChannelsActions = {};

export function useChannelsActions(): ChannelsActions {
  return {};
}
