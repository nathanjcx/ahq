'use client';

/** Meetings actions. Filled by the meetings UI workstream: type, offline stubs, and the hook, kept in step. */
export type MeetingsActions = Record<never, never>;

export const offlineMeetingsActions: MeetingsActions = {};

export function useMeetingsActions(): MeetingsActions {
  return {};
}
