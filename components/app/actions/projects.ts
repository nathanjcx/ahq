'use client';

/** Projects actions. Filled by the projects UI workstream: type, offline stubs, and the hook, kept in step. */
export type ProjectsActions = Record<never, never>;

export const offlineProjectsActions: ProjectsActions = {};

export function useProjectsActions(): ProjectsActions {
  return {};
}
