'use client';

/** Schedule actions. Filled by the schedule UI workstream: type, offline stubs, and the hook, kept in step. */
export type ScheduleActions = Record<never, never>;

export const offlineScheduleActions: ScheduleActions = {};

export function useScheduleActions(): ScheduleActions {
  return {};
}
