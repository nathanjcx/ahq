'use client';

import type { PageProps } from './page-props';

/** Open incidents, shown above every page while one is open. Filled by the triage workstream. */
export function IncidentStrip(_props: Pick<PageProps, 'dashboard' | 'actions' | 'run' | 'go'>) {
  return null;
}
