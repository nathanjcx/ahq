# QA handoff requirements

The completed PIN-184 work must provide a patch workspace plus saved patch and test evidence. PIN-185 must verify that completed workspace independently. Record the tested path, the command, both principal case results and any failure. If the patch is absent, keep QA waiting. This checklist is a request, not evidence that a patch exists or tests passed.
