# Platform v4 progress

Running log for the pass described in [the plan](platform-v4-plan.md). Updated at every phase boundary. Newest entries at the bottom.

## Phase 1: contracts and tooling (done, September 12, 2026)

Branch `platform-v4`, commit `f99ef34`.

- Floors and projects separated: the old `projects` table and every identifier became `floors`; real `projects` and `milestones` tables added.
- Schema: `shifts`, `reports`, `taskSummaries`, `memories`, `channels`, `posts`, `calendarEntries`, `meetings`, `meetingTurns`, `auditFindings`, `alerts`, `notifications`, `pushSubscriptions`, `workspaceSettings`; new fields on `installations` (floor, name, kind, overnight model), `tasks` (project, milestone, cadence, deadline, dependencies, `waiting` and `blocked` statuses), `floors.reserved`.
- `lib/contracts/` and `lib/ui-api/` split per domain with an index; `components/app/actions/` split the same way. Each phase-two workstream owns its own file in each.
- `convex/work.ts` split into `convex/lib/tasks.ts` and `convex/lib/posts.ts`.
- ESLint with type-aware rules, react-hooks, and import order; `npm run check` now runs lint. Three React compiler rules are warnings until the code-quality workstream clears them.
- Office lab: `app/office-lab` (guarded by `QA_FIXTURE=1`), five deterministic baselines under `web-tests/lab/baselines`, a frame-time probe with a configurable budget, `npm run test:lab`.
- Verification: typecheck, lint, 121 unit tests, production build, two clean baseline runs.

## Phase 2: backend (merged, September 12, 2026)

Five Opus agents in isolated worktrees: memory and janitor; schedule, calendar, plan; projects, roadmap, dependencies; meetings and audit findings; channels, alerts, notifications, triage. Known reconciliation points at integration: `ensureReservedInstance` (memory, meetings, channels each carry a copy), `isAttendedTime` (schedule owns `lib/time.ts`; triage carries a private copy), `Dashboard` contract additions from the schedule workstream, additive schema changes reported by each agent.

### Merged

- `agent/memory` (commit `6fc05cd`): memory tables, people and janitor operations, the working memory compiler, 11 new tests. Follow-up merged (`fcb0b4d`): `contest` names the competing entry and `resolveContest` acts on both sides.
- `agent/meetings` (commits through `170bb56`): meetings engine on hidden per-attendee session tasks, audit findings lifecycle, `ensureReservedInstance`. Job kinds for the worker: `meeting_prep`, `meeting_answer`, `meeting_wrapup`, `audit_run`.

- `agent/projects` (commit `54b3c95`): projects, milestones, roadmap proposal validation, task dependencies with waiting and blocked states released from `recordEvents` and `tasks:cancel`, 20 new tests.

- `agent/channels` (commits through `863cc63`): channels and posts replace floor boards, alerts with signed intake and dedupe, notifications ledger, triage floor and authority, five new routes, 9 new tests.
- `agent/schedule` (commits through `33fdeec`): pure time math and planner, settings, five-minute tick, shifts and reports, calendar with derived entries and agenda suggestions, plan projection, 46 new tests. Job kinds: `start_shift`, `review_shift`, `prep_turn`, `curation_run`, `audit_run`, `triage_run`, plus `meeting_prep`, `meeting_answer`, `meeting_wrapup`, `email_classify`.
- Integration fixes by the primary thread: same-millisecond ties in post paging, unread markers, and memory ordering made deterministic on Convex creation time.
- State after merge: typecheck clean, 213 unit tests, 36 files.

### Reconciliation (phase 2b, merged)

- `tasks.kind` field (`work | meeting | audit | curation | triage`) so the dashboard hides session-only tasks; one shared "session task without a start job" helper in `convex/lib/tasks.ts` for meetings, audits, curation, and triage.
- Janitor creation onto `ensureReservedInstance` (convex/lib/audit.ts); triage's private copy too.
- Marketplace list must exclude reserved versions (`category: 'Reserved'`).
- Findings and escalations posted to channels once the channels module lands; the calendar must consume `confirmOutcome`'s `meetingRequest`.
- The dashboard task projection must carry `projectId`, `cadence`, `deadlineAt`, `dependsOn`; `releaseDependents` scans `by_status` for waiting tasks, so an index on dependencies is worth adding when that table grows.
- The scheduler must call `ensureMeeting` at the prep lead, `ensureAuditRun` after hours, `ensureJanitor` per workspace, and order shifts with `openFindingsFor`.

Phase 2b outcome (`agent/integrate`): one reserved-instance helper in `convex/lib/reserved.ts` used by janitor, auditor, and triage; one attended-hours check and one settings owner in the schedule module; `tasks.kind` and `sessionKey` with `openSessionTask` for meeting, audit, and standing sessions; the dashboard hides session tasks and carries project, cadence, deadline, and dependency fields; the tick creates reserved staff, opens audit runs after hours, and prepares meetings at the lead; reports, findings, escalations, and contested claims post to channels; every meeting is booked through `createMeetingEntry`; new indexes on `usageReports` and `tasks`; `floorPosts` removed. 221 tests.

## Phase 3: infrastructure (in progress)

Four Opus agents: worker turns and internal tool servers with role enforcement (`agent/runtime`); marketplace listings, instances, hiring policy, studio preview (`agent/marketplace`); code quality helpers, lint to zero outside the office, CSS consolidation, typed boundaries (`agent/quality`); 3D rooms and props with new baselines (`agent/rooms`, started with phase 2b).

- Merged `agent/marketplace` (commits through `2ba4453`): listings as the unit with visibility, evidence, and counters; instances hired by count with names and floor staffing; hiring policy with requests; upgrade with readiness rollback; studio preview and version diff; `lib/instructions.ts` as the shared instruction composer. 236 tests. Note: `capacity.instances` in the plan projection still counts reserved instances while the hiring cap counts workers only; reconcile in the UI phase.
- Merged `agent/rooms` (commits through `8d4f6c0`): records basement, boardroom, triage floor, lobby calendar wall, floor props (memory binder, notebooks, contested and findings folders, task board with dependency strings, incident beacon, overnight lamp), figure kits for janitor, auditor, and triage; six new baselines and three re-accepted after an overlay relayout fix; 16 pure layout tests. 252 tests. Open item for phase five: draw calls per frame are about 2,000 on a floor at 1440 (pre-existing), far above the plan's 400; the performance workstream must instance and merge geometry before the probe can enforce that budget.
- Merged `agent/runtime`: worker turns for every job kind (`start_shift`, `review_shift`, `meeting_*`, `curation_run`, `audit_run`, `triage_run`, `email_classify`, `plan_project`), internal tool servers in the gateway (`astra_memory`, `astra_floor`, `astra_shift`, `astra_audit`, `astra_janitor`, `astra_triage`) with the role matrix in `lib/server/agents.ts`, one untrusted fence in `lib/server/untrusted.ts`, and a nine-scenario runtime day harness with a fake clock. Primary thread wired the two hooks the runtime flagged: `projects:create` and `replan` enqueue `plan_project` through `enqueuePlanningFor`, and Gmail inbox ingestion enqueues `email_classify`. Project views now count only work tasks so the planner's standing session task is invisible. 261 tests.
- Merged `agent/quality` (commits through `c5d8e90`): React compiler findings cleared outside the office (two real bugs: a replay rerender loop and `[object Object]` in the JSON view); `lib/text.ts` and `components/shared/time.ts` as the one home for text and clock formatting; `app/globals.css` down from 4,029 to 1,006 lines with one stylesheet per page directory; typed draft input derived from the Convex function; `components/README.md` records the conventions. The three React compiler rules are errors again; 19 errors remain, all in `components/office`, for the office workstream in phase four. 281 tests.
- Phase four scaffold by the primary thread: page ids `projects`, `calendar`, `records`, `audit`, `triage` with placeholder pages; `PageProps` in `components/app/page-props.ts` with project and meeting selection in the shell; `useUiQuery` (`components/shared/use-ui-query.ts`) answers from `app/qa/fixtures/*` under the fixture route so page-owned queries can be photographed; `ChannelFeed` and `IncidentStrip` stubs at their final import paths.

## Phase 4: interface (in progress)

Six Opus agents in isolated worktrees on top of the scaffold: projects and roadmap (`agent/projects`); calendar and boardroom (`agent/calendar`); records, audit, and settings sections (`agent/records`); floors, channels, triage, incident strip, and notifications (`agent/floors`); employees, hire by count, marketplace, and studio (`agent/employees`); the live office scene, day replay, lint to zero, and draw-call instancing (`agent/office`). A docs agent (`agent/docs`) rewrites architecture, operations, verification, and security for the backend that is final. The primary thread switched the worker to the shared instruction composer (`db9caa0`).
- Merged `agent/docs` (`e82affc`): architecture, operations, verification, and security rewritten against the code; `project-floors.md` reduced to a pointer. Gaps it found, for phase five (backend) and the running workstreams: the emergency rule cannot be reached because nothing re-pages on a schedule and the attempt count only looks back 20 minutes; only in-app notification delivers; `auditPolicy` is stored and never read; `overnightPolicy: 'off'` still runs the nightly audit; two job-kind lists are kept in step by hand; the `lobby` reserved floor and the `task` memory scope are unreachable; the alert secret is set through a platform-admin route; `README.md` still describes project floors. Already assigned: `capacity.instances` (employees workstream), activity model and draw-call assertion (office workstream).
- Merged `agent/records` (commits through `3a10846`): Records page (scope shelves, conflicts queue, chains, composer, budgets, janitor log from `memory:janitorLog`), Audit page (nights, per-instance documents worded from the audit policy, escalations, filters), Settings panel in five sections (Workspace, Schedule, Plan and budgets, Policies, Standards) saving through one action. Noted for the employees workstream: the overnight model lives on the installation and needs its control there. 283 tests.
- Merged `agent/projects` (commits through `e5d2992`): projects list and detail, new-project sheet, proposal review with answerable bottleneck questions (hire onto a floor, move deadlines, accept), roadmap timeline with floor lanes and dependency strings, held-up tasks with what they wait on, `projects:tasks` query and `channelId` on the project view. 289 tests. Reconcile at the end of phase four: `hireForProject` in the projects actions against the marketplace workstream's hire-with-count; the project deadline rides in the brief because `projects.create` has no deadline field (decide whether to add one).
- Merged `agent/calendar` (commits through `d277c50`): week board with instance and tower rows, day timeline with lane packing, agenda list on phones, schedule and reschedule sheet with suggested agenda and the attended-hours warning, the boardroom view (prep reports, transcript with per-question usage, composer, confirmable outcomes). 289 tests. For phase five: `meetingTurns` needs a `usage` field written by `recordAnswer` and returned by `meetings.get`; the QA fixture dashboard needs a `schedule` summary so the overnight row photographs.
- Merged `agent/employees` (commits through `b7f6ed8`): employees page around instances (grouped by version and floor, reserved staff, retired toggle, rename, move, overnight model, feed, notebook, findings, upgrade, retire), the shared hire sheet with count, names, floor, overnight model, capacity and cost effect, hire requests for admins, marketplace filters, version history and diff, studio preview; `capacity.instances` now counts workers only. Primary thread merged the fixture answers so each Convex query has one: notebooks and instance findings join the memory and audit fixtures, the projection stays with projects. 292 tests. Shell items for the end of phase four: the phone sidebar needs overflow scrolling; a per-instance channel does not exist, so the feed tab shows the floor channel; `hireRequests` carries no names or overnight model.
- Merged `agent/floors` (commits through `ce8e541`): `ChannelFeed` (kinds, unread line, paging, @mention composer, compact mode), floor page on channels with Feeds and Binder tabs, Triage page (alert inbox and timeline with allow-list versus emergency calls, paging sentence, incident reports, intake setup), incident strip, notifications bell wired to the shell. Triage backend gained `acknowledgeAlert`, `assignFloors`, `timeline`, `incidentReports`, `intake`, `acknowledgeNotifications`, and one attempt-window constant. 295 tests. For phase five: a `file_incident_report` tool for the emergency path; a flag for contested-claim posts instead of a text prefix; the QA fixture needs a triage employee and tasks with deadlines, cadence, project, and waiting or blocked states.
- Integration pass by the primary thread after five UI merges: the phone sidebar scrolls (thirteen pages), the unconfigured shell mounts page queries under a placeholder client with empty fixtures, the every-page and smoke walks carry the slow budget. Browser suites against one dedicated server: smoke 2/2, visual 8/8, projects 3/3, calendar 2/2, records 2/2, floors 4/4, employees 3/3 (one screenshot protocol error in a ten-minute run passed alone). Screenshots reviewed: projects, calendar, records, audit, triage, employees, office at desktop; calendar at phone.

## Phase 5: closing (in progress)

- `agent/backend` (Opus) fixes the gaps the docs and UI workstreams surfaced: the emergency rule reachable with scheduled re-paging and a first-attempt-anchored count, a `file_incident_report` tool enforced at run close, `auditPolicy` and `overnightPolicy` honoured by the planner, one job-kind list, per-question meeting usage, a project deadline field, hire requests carrying names and overnight model, a flag on contested-claim posts, a workspace-admin alert secret, web push delivery, and the small unreachable values. The office workstream is finishing its performance pass. A UI reconciliation pass follows the backend merge (hire actions folded together, the new project deadline field in the sheet, employee feeds, the QA fixture's schedule summary, project and triage rows).
- Merged `agent/office` (commits through `8a99dcb`): office lint to zero (every React compiler finding fixed at source; the whole repo lints clean for the first time), the activity model for shifts, meetings, audits, triage, janitor, planner, waiting and blocked work, a pure day replay with a scrubber (`OfficeDay`, not yet mounted by the floor page), static geometry merged per material family so draw calls at 1440 fell from about 2,100 to about 300 on a floor, the probe now asserts under 400 calls, four new baselines (meeting-live, audit-night, incident, day-replay) with the eleven existing ones unchanged. 318 tests. Lab suite on the merged tree: 16 baselines pass; the frame-time probe failed once under a load average above 18 while another agent ran test suites, to be rerun idle. The current branch is also pushed to the private repository `trystaff/staff-ai` (branches `main` and `platform-v4`).
- Merged `agent/backend` (commits through `1de1d5f`): the emergency rule is reachable (`lib/paging.ts`: attempts anchored on the first standing page, the tick re-pages every seven minutes outside attended hours, a `page_alert` worker job delivers without a model turn, acknowledgement resets the gate); `file_incident_report` on `astra_triage` enforced at run close with a `missing` placeholder and an escalation; `auditPolicy` hard holds a flagged instance to its findings shift; `overnightPolicy` off/audits_only/cheap honoured; one job-kind list in `lib/jobs.ts`; per-question meeting usage; `projects.deadlineAt`; hire requests carry names and overnight model; contested posts carry `flag`; `remember` accepts the task scope; the `lobby` reserved value removed; web push through `web-push` under VAPID variables; the alert secret set by a workspace administrator (primary thread moved the route onto the actor mutation and dropped the platform-admin one). 334 tests.

## Phase 6: review (in progress)

Three Fable read-only reviews ran against the merged backend while the UI reconciliation pass (`agent/reconcile`) runs. Blockers found: a page to N people counted as N attempts, so the emergency allow-list could open after one page in a workspace with three subjects; a triage run never re-ran, so the emergency tools were never offered; a daily task's second shift was failed because the monitor completed the task after the first shift; daily tasks were also started at once as ordinary sessions and fired completion side effects per shift. Should-fix: in-app delivery counted as a page; the Working memory block and meeting agenda lines entered prompts unfenced; the janitor's merge activated unapproved claims; supersede archived an active claim while the replacement waited; triage wrote through members' private connections; push endpoints were an SSRF sink; per-question meeting usage recorded cumulative session usage; channel paging died after the second page; one open alert starved other alerts of triage; the tick was one transaction over all workspaces; and smaller items. All are being fixed with tests on `agent/fixes` (Opus); the scene-versus-baselines review runs after the reconciliation pass lands.
- Merged `agent/fixes` (16 commits): every review finding addressed with a test that was red before and green after. Pages counted by batch rather than by recipient; triage runs keyed by ledger state and gate so a fresh run sees an open gate; `claimJobs` reopens a completed task for shift kinds; daily tasks start as shifts only, and a completed daily shift releases nothing downstream; in-app delivery no longer counts as a page and an emergency allow-list needs a real transport; Working memory and agenda lines fenced at the worker edge; per-question meeting usage recorded as a delta; channel paging ranges on the index with strictly increasing post times; janitor merge inherits its inputs' standing and a proposed claim cannot supersede; `recall` returns active claims only; triage writes only through workspace-visible connections; push endpoints resolved through the public-only lookup and capped at ten per subject; unsigned alert traffic metered by sender; the tick runs per workspace; review shifts only for waiting tasks; estimates read the workspace's own history; both shift dates count toward capacity. Known gaps kept in architecture.md: the curation batch key and a milestone with a failed task. 360 tests. `docs/verification.md` counts to refresh after the reconciliation pass.
- Merged `agent/reconcile` (7 commits): one hire action and one hire sheet used by projects, employees, and marketplace; the project deadline as a field; hire requests with names and overnight model; the instance's own feed; the day replay mounted on the floor page with the board built from real tasks; reserved staff labelled Janitor, Auditor, Triage in the office; the QA fixture carrying schedule, deadlines, cadence, waiting and blocked tasks, a contested post, and a missing incident report; docs placeholders replaced. 365 tests.
- Final verification on the merged tree (`f2a4410`): typecheck clean; `npx eslint .` zero problems; 365 unit tests in 45 files; `npx next build` clean; browser suites 22/22 against a dedicated server; office lab 16/16 with the probe under budget. Pushed to `trystaff/staff-ai`. Waiting on the scene review before the fast-forward to `origin/main`.
- Scene review (Fable, read-only): blocker, the records room, boardroom, triage floor, calendar wall, memory binder, contested folder, and task board were reached only by lab presets, never by the live product; should-fix, the real day replay carried no tasks, `off_shift` and `reporting` needed a schedule the live floor never passed, `arriving` sat below the session rule, live daylight froze at mount, every desk lamp lit after hours, the board card hid the back row, figures could share the door, off-shift figures stayed seated, the notice hung on the glass, plus nits on finding choice, binder colours, the probe's viewport claim, and 20 Hz per-figure state. Fixed on `agent/scene` (Opus): each room routed to the page it belongs to and derived from real data, with the rest of the findings.

## Release track (September 13, 2026)

- Deployed to the trystaff org: Convex production `combative-pig-574`, Railway services `web`, `worker`, `gateway` from `trystaff/staff-ai` `main`, `app.trystaff.ai` with a valid certificate, R2 bucket `staff-ai-artifacts` write-tested. Details in `docs/deployment.md` section 6.
- Production-readiness audit (Fable, read-only) and `agent/harden` (Opus, merged, 370 tests, all three images built and signal-tested in Docker): lazy OpenAI client so the worker reports missing configuration instead of crash-looping; failed and uncertain job counts in the worker's health and logs; one escalation post when the daily cap holds work; each service runs its process directly as PID 1; runtime images without dev dependencies and without a Next build for worker and gateway; rate limits keyed on the last forwarded hop and charged only after signature verification; VAPID documented; dead build args removed; Node pinned; the gateway's health checks Convex. Open for the primary thread: the canonical-host redirect after the WorkOS merge.
- In progress: `agent/scene` (rooms live), `agent/workos` (Clerk to WorkOS AuthKit), `agent/brand` (Staff AI name and blue palette).
